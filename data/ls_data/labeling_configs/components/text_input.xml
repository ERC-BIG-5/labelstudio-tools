<View>
    <Style>
        .{{{className}}} .lsf-richtext__line {font-size: {{{fontSize}}}px;}
    </Style>
    <View className="{{{className}}}">
        <Text name="gap1" value=" "/>
        <Header size="4" value="{{{title}}}:"/>
        <Text name="text_{{{index}}}" value="$text_{{{index}}}" selectionEnabled="false"/>
    </View>
</View>